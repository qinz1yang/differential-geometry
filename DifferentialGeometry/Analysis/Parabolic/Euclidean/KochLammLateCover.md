# KochLammLateCover

## Proved facts

- the heat-kernel/source product is integrable on every selected piece inside
  one late source cylinder;
- an arbitrary finite radius-`R` ball cover gives a bound proportional to its
  cardinality;
- overlaps are removed by an inductive measurable split, so no disjointness
  assumption is imposed on the supplied cover.

This layer is parameterized by the cover. It does not duplicate or import the
canonical `exists_shell_cover`. The focused check passes without local
warnings. The exact theorem `ricci_flow_forward_unique` remains **0%**.
