# KochLammFluxTail

## Proved facts

- the integrable half-Gaussian remainder scales with the same parabolic
  exponent as the full first-derivative majorant;
- a scaled lower distance extracts the real-power factor
  `(exp(-k^2/8))^p`;
- integration on a selected terminal spatial set preserves `klD1Exp` and
  yields the corresponding flux-tail mass bound.

The exact theorem `ricci_flow_forward_unique` remains **0%**.

The focused Lean check passes without local warnings. The file contains no
proof placeholders or new foundational assumptions.
