# KochLammLateSeries

## Proved facts

- `klLateWeight d k` is the exact `(5(k+1))^d exp(-k^2/4)` shell weight;
- `klLateWeight_sum` proves this sequence is summable by comparison with a
  polynomial times `exp(-k/4)`;
- `klLateSeries d` is its finite, nonnegative dimension-only total mass.

This producer is independent of every quantitative-cover module. The focused
check passes without local warnings. The exact theorem
`ricci_flow_forward_unique` remains **0%**.
