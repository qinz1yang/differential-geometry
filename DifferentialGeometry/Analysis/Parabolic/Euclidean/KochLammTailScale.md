# KochLammTailScale

## Proved facts

- the split-Gaussian terminal mass is positive;
- its Hölder-dual root has exactly the Koch--Lamm late-source radius scale;
- the root of the terminal far-set kernel bound gains `exp (-k^2 / 4)`.

This is the scale-cancellation input for the parameterized finite-cover
Hölder estimate. The focused check passes without local warnings. The exact
theorem `ricci_flow_forward_unique` remains **0%**.
