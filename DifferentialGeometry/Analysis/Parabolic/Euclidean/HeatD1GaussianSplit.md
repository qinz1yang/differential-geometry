# HeatD1GaussianSplit

## Proved facts

- `baseD1Maj` is bounded by a dimension-normalized Gaussian with exponent
  `-normSq/8`;
- its exact Gaussian splits into an extracted far-field factor and an
  integrable half-Gaussian factor;
- the real power tail bound yields `exp(-p R^2/8)` before taking the dual root.

This is the pointwise and spatial-mass input for the late flux shell estimate.
The exact theorem `ricci_flow_forward_unique` remains **0%**.

The focused Lean check passes without local warnings. The file contains no
proof placeholders or new foundational assumptions.
