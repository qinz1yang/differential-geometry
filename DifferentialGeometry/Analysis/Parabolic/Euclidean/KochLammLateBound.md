# KochLammLateBound

## Proved facts

- `realLpFactor_eq`: identifies the real power-integral factor with
  `eLpNorm.toReal` at a finite positive exponent.
- `klLateKern_fac`: bounds the local terminal-cylinder kernel factor by the
  exact global scaled factor.
- `klLateSrc_fac`: converts `KLSource0.late_lq` into the inverse real radius
  scale.
- `klLateNear_norm`: cancels the two radius factors and gives a
  radius-independent near-field bound.

No uniform-in-time spatial source norm is used.  The remaining late ordinary
source work is the parameterized far-shell theorem and summation.

The exact theorem `ricci_flow_forward_unique` remains **0%**; the heat-map
fixed point, geometric realization, continuation, and gauge ladder remain
downstream.
