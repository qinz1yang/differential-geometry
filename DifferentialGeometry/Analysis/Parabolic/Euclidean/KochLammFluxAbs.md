# KochLammFluxAbs

## Intended facts under focused verification

- absolute integration of the directional kernel/source product on one far
  selected piece obeys the same scale-free Gaussian estimate as the norm of
  its Bochner integral;
- disjoint finite-cover induction preserves that absolute estimate;
- the `k`-th shell absolute integral is bounded by the canonical cover
  polynomial times `exp(-k^2/8)`.

The shell result is the exact hypothesis needed to assemble full terminal
Bochner integrability.  The exact theorem `ricci_flow_forward_unique`
remains **0%**.
