# KochLammLatePiece

## Proved facts

- a selected terminal spatial piece inside one radius-`R` ball inherits the
  local `KLSource0.late_lq` bound;
- joint space-time Hölder applies on that selected piece;
- if the piece is at distance at least `kR`, its contribution is bounded by
  `exp (-k^2 / 4)` times a dimension-only constant and `A_q`.

No cover or shell partition is chosen here. The focused check passes without
local warnings. The exact theorem `ricci_flow_forward_unique` remains **0%**.
