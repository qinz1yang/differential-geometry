# KochLammLateShell

## Proved facts

- the `k`-th half-open spatial shell `[kR,(k+1)R)` is measurable and lies in its outer
  closed ball;
- shell membership gives the lower distance `kR` required by the Gaussian
  tail bound;
- any finite radius-`R` cover satisfying the canonical
  `(5(k+1))^dim` cardinality estimate yields the corresponding polynomial
  times Gaussian shell bound.

The cover remains an input, so this file neither imports nor duplicates
`exists_shell_cover`. The focused check passes without local warnings. The
exact theorem `ricci_flow_forward_unique` remains **0%**.
