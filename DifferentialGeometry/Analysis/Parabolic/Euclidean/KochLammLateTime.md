# KochLammLateTime

## Proved facts

- `klHeatExp_add`: the integrated terminal singularity has exponent
  `2/(n+2)`.
- `klTermScale_exp`: taking the Hölder-dual root changes this to
  `2/(n+4)` in observation time.
- `klTermTime_int` and `klTermTime_set`: exact interval and restricted-set
  formulas for the terminal half-slab power integral.

These identities are the quantitative bridge behind the spatial-radius
factor `R^(4/(n+4))`, which must cancel the inverse scale supplied by
`KLSource0.late_lq`.

The exact endpoint `ricci_flow_forward_unique` remains **0%**.  This file is
strictly analytic machinery; the global shell sum, heat-map solver, geometric
realization, and gauge argument remain downstream.
