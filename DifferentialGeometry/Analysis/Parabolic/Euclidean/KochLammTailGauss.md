# KochLammTailGauss

## Proved facts

- integrability and exact mass of the split `p/2` Gaussian;
- exact translated integral of its scaled kernel;
- a pointwise far-field bound for `H^p` with decay
  `exp (-(p*k^2)/4)` when the scaled radius is at least `sqrt 2 * k`.

This layer is independent of finite-cover and shell-count APIs.  The next
layer will impose terminal-slab geometry and parameterized cover data.

The exact theorem `ricci_flow_forward_unique` remains **0%**.
