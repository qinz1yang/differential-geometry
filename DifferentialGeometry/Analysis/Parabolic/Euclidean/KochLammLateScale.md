# KochLammLateScale

## Proved facts

- positivity of the scalar terminal mass factor;
- extraction of `klLqScaleR R ^ klQDual` at time `R^2`;
- after the Hölder-dual root, the exact identity
  `kernel factor = klLate0C V * klLqScaleR R`.

This is the precise scale cancellation required by the late ordinary-source
estimate.  It introduces no horizon-dependent or source-dependent constant.

The exact endpoint `ricci_flow_forward_unique` remains **0%**.  The local
source-factor conversion and the parameterized far-shell sum remain before
the rough heat-map solver and geometric gauge ladder.
