# KochLammLateTail

## Proved facts

- terminal-slab geometry converts a spatial lower radius `kR` into the
  scaled lower radius `sqrt 2 * k`;
- the translated split-Gaussian majorant is integrable;
- `klTermTail_pow` integrates the far-field decay over any measurable
  spatial set, without choosing a cover.

The next layer will combine this kernel-power estimate with local source
Hölder bounds and parameterized finite-cover/cardinality data.

The focused check passes without local warnings. The exact theorem
`ricci_flow_forward_unique` remains **0%**.
