# KochLammLateAbs

## Proved facts

- joint space-time Hölder bounds the integral of the norm on one far selected
  piece by the same scale-free Gaussian factor as the vector integral;
- the absolute-integral bound sums over arbitrary overlapping finite covers;
- the parameterized shell estimate has exact
  `(5(k+1))^dim exp(-k^2/4)` decay.

This is the absolute-convergence input for identifying the shell series with
the full terminal potential. The exact theorem `ricci_flow_forward_unique`
remains **0%**.

The focused Lean check passes without local warnings, and these producers are
free of proof placeholders and new axioms.
