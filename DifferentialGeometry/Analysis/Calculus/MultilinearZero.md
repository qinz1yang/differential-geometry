# MultilinearZero

`cml_deriv_zero` records the cancellation in the derivative of a time-varying
continuous multilinear map evaluated on moving slots when the map vanishes at
the base time.  It is a generic analysis-layer lemma, proved using Mathlib's
multilinear derivative rule.  Focused verification passed.
