# LieCorr0JointSmooth

## Role

This module proves joint smoothness of the zeroth-order DeTurck correction
coefficient along the realized metric path.

## Verified state

`lieCorr0_path_joint` is public and sorry-free. Focused verification and the
named module build pass. The theorem supplies the interval-integrability input
used by the public three-arm path construction.

This producer is complete (100%). The mixed `H3 -> H1` endpoint remains
unstated and theorem-level 0%.
