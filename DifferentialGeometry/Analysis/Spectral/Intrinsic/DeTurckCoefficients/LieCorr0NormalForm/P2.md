# Second lower-order normal form

The public theorem `nf_p2` expands the quadratic connection-difference block.
Its only structural hypothesis is symmetry of `f`; the old uniform generator
parameter bundle was removed from the statement.

Focused verification passed without warnings or `sorry`.

This is an algebraic input to the lower Lie readout, not the mixed H3 to H1
estimate itself.

