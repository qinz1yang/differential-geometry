# Fourth lower-order normal form

The public theorem `nf_p4` expands the reanchored connection-difference block.
Eight generated index permutations are factored through two private generic
lemmas, and the statement retains only the symmetries it uses.

Focused verification passed without warnings or `sorry`.

This is an algebraic input to the lower Lie readout, not the mixed H3 to H1
estimate itself.
