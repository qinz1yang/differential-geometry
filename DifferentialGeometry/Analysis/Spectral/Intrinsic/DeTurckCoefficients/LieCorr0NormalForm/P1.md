# First lower-order normal form

The public theorem `nf_p1` replaces twelve generated reindexing lemmas with
four generic finite-sum identities.  Its only structural hypothesis is symmetry
of the varied metric component `f`; all unrelated generator hypotheses were
removed.

Focused verification passed without warnings or `sorry`.

This is an algebraic input to the lower Lie readout, not the mixed H3 to H1
estimate itself.

