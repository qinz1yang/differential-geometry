# Shared zeroth-order DeTurck scalar definitions

## Role

This module contains the finite-index expressions shared by the remaining
normal-form chain.  They are algebraic representations only; no regularity or
Sobolev assumptions occur here.

The former generated names `O0F`, `V0F`, and `D1RF` were normalized to
`o0F`, `v0F`, and `d1RF`.  Dummy parameters are explicitly marked so the
transitional uniform calling convention does not masquerade as mathematical
dependence.

## Verification

Focused verification passed without `sorry`.

## Frontier

The normal forms for `o0F`, `v0F`, and `d1RF` still have to be extracted before
the algebraic master identity can be made public.

