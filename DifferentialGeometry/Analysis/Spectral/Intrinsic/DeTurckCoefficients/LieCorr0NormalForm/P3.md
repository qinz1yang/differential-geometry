# Mixed lower-order normal form

The public theorem `nf_p3` expands one mixed connection/background-connection
half.  `nf_p3_swap` is derived by exchanging the two free tensor indices rather
than replaying a second generated proof.  The statement keeps only the metric,
cometric, and connection symmetries actually used.

Focused verification passed without warnings or `sorry`.

This is an algebraic input to the lower Lie readout, not the mixed H3 to H1
estimate itself.

