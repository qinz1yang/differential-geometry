# Covariant-derivative correction normal form

## Role

`V0.lean` extracts the complete finite-index normal form of the zeroth-order
covariant-derivative correction.  Its 28 generated leaf lemmas remain private;
only `nf_v0` is public.

The module has no manifold, regularity, Sobolev, metric-ball, or high-norm
assumptions.

## Verification

Focused verification passed without `sorry`.

## Frontier

The `d1RF` and residual `rz` normal forms still precede the algebraic master
identity.  The mixed H3 to H1 theorem remains unstated and unproved.
