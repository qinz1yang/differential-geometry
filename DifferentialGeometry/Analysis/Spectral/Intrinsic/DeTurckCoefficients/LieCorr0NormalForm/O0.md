# Inverse-metric correction normal form

## Role

`O0.lean` extracts the complete finite-index normal form of the order-zero
inverse-metric correction from the settled private DeTurck algebra chain.  The
38 generated reindexing lemmas remain private; only `nf_o0` is public.

The module contains no manifold, regularity, Sobolev, metric-ball, or high-norm
assumptions.  Its large uniform parameter list is inherited by this internal
algebraic identity and is not intended as the final geometric API.

## Verification

Focused verification passed without `sorry`.

## Frontier

The `v0F`, `d1RF`, and residual `rz` normal forms still precede the algebraic
master identity.  The mixed H3 to H1 theorem remains unstated and unproved.
