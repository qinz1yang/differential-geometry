# Mixed-tensor H1 to L6 estimate

Status: proved and focused-check clean.

`h1_lp6_fiber_rs` is a general mixed `(r,s)` estimate on a closed
three-manifold.  It bounds the real `L⁶` norm of the intrinsic fibre norm of
`S.toCcTensor` by a uniform non-negative constant times `‖S‖`, for
`S : SmoothCcTensorH1 g r s`.

The proof uses the constant-first scalar closed-manifold Sobolev embedding,
the existing chart-component `H¹` bounds, `fiber_sq_le_comps`, finite
square-root reconstruction, finite `eLpNorm` Minkowski, and the standard
`eLpNorm.toReal` bridge.  No covariant-only spectral realization is used, so
the result applies directly to mixed operator coefficients.

No mathematical route failed.  During elaboration, two representation
bridges had to be made explicit: `ENNReal.ofReal 2` in the chart Sobolev API
and the canonical Riemannian volume-measure unfolding.

Producer completion: 100%.  This closes only the mixed `H¹ -> L⁶` analytic
cell; `ricci_flow_unif_existence` remains a separate endpoint until its whole
low-regularity solver and same-horizon regularity chain are assembled.
