# EntropyMix

## Status

`negMulLog_add_le` proves the scalar subadditivity inequality needed to add a
small positive background density to a compactly supported Perelman cutoff.
It is a pure real-analysis producer and adds no geometric assumptions.

Focused verification and targeted module verification passed.
