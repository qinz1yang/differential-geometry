# ConjugatingFlowProperties status

## Proved

- `evalForm_joint` exports the two-variable Fréchet derivative needed to
  differentiate a metric family evaluated on two moving push-forward slots.
- `flow_slot_deriv` proves the moving-slot derivative for any jointly smooth
  diffeomorphism family generated by the negative of an arbitrary smooth
  time-dependent vector field: the contribution is `-ℒ_X g`.
- `flow_slot_pos` proves the corresponding positive-generator form, whose
  contribution is `ℒ_Y g`.
- `conjugating_flow_flat_data` now obtains its original DeTurck-specific result
  by specializing the general theorems.  Its public statement is unchanged.
- The focused source check passes without warnings introduced by these edits.

## Failed verification route

A named exported-artifact refresh reached a shared deep dependency whose
artifact path exceeds the Windows path limit, while another lane was restoring
that same artifact.  The overlapping build was stopped without killing the
other lane.  This is an artifact/cache obstruction, not a source proof error;
the focused source check above is green.  The exported artifact must be
refreshed only after the shared dependency restoration completes.

## Role in forward uniqueness

The moving-slot and joint-evaluation machinery for the reverse
Ricci-to-Ricci--DeTurck pullback identity is now proved.  This machinery is
separate from the endpoint: `ricci_flow_forward_unique` remains 0% until its
exact theorem is proved and axiom-checked.

## Remaining frontier

The next conditional theorem combines these identities with the Ricci-flow
metric derivative and Ricci/Lie naturality to show that a gauge family solving
the standard positive DeTurck velocity equation pulls a Ricci flow back to a
Ricci--DeTurck solution.  The genuine analytic blocker after that theorem is
short-time harmonic-map heat-flow existence, joint smoothness, and
diffeomorphism preservation for an arbitrary smooth Ricci flow.

Direct Kotschwar energy uniqueness was also audited as a distinct route.  The
repository has connection and curvature component evolution ingredients, but
not the coupled difference evolution system, cross-metric energy estimate, or
initial-edge regularization needed to close it.  It is therefore presently
longer than completing the conditional gauge identity and then adding the
map-valued parabolic producer.

## Honest progress

- General moving-pullback differentiation machinery in this file: 100%.
- Reverse gauge identity consumer: not yet verified in this file.
- Harmonic-map heat-flow gauge construction: 0%.
- Exact forward-uniqueness endpoint: 0%.
