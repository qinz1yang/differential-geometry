# MetricPreconvWindowAllPt

`windowGInfAll_pt` (pointwise companion of `windowGInfAll`) is GREEN, sorry-free, axioms `[propext, Classical.choice, Quot.sound]`; the planned route "extract via `metricDerivNorm_le_metricDerivNormSupOn`" was unusable (that lemma is `[CompactSpace M]`/`Set.univ`/order-0/`gRef = gInf` only) and `sSup < ε` alone is junk-compatible (Real `sSup` of an unbounded set is 0), so pointwise is NOT black-box derivable from the sup form without `BddAbove`.
Working route (no continuity API needed): singleton compacts make the sup set a finite image of `Set.Iic p` (`derivNorm_le_sup_sing`, genuine per-point smallness), then triangle through a singleton-tail comparison metric `gSeq (φ m0) t` plus `hbdd` along `φ` (`derivNorm_le_cov_add`: seminorm ≤ sum of `metricCovDerivNorm`s, via `sqrtNormSq0S_add_le` + `normSq0S_neg`; NB `rw [sub_eq_add_neg]` fails keyed matching on the tensor fiber — use closed-term `have` equations) discharges `BddAbove` of the `(K, p)` sup set, and `le_csSup` finishes.
