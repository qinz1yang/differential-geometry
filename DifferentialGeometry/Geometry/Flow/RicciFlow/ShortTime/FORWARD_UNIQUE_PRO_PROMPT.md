# GPT Pro consult prompt — black box (B) forward-uniqueness statement + route ruling (2026-07-24)

Send in a fresh chat of the ChatGPT project "Lean Pro Consult Handoff".

---

I am working in a large Lean 4/mathlib project. Do not write code first. This is a STATEMENT-BOUNDARY + ROUTE/DESIGN review for the Ricci-flow forward-uniqueness frontier: rule on a statement surgery, rule between two proof routes, and give a small lemma frontier.

Target theorem:
`ricci_flow_forward_unique` — one of the two `sorry`s of `DifferentialGeometry/Geometry/Flow/RicciFlow/Evolution/ExtendViaUniqueness.lean` ("black box (B)"), line 175: two flows `g₁ g₂ : ℝ → SmoothRiemannianMetric I M` on a CLOSED manifold (dimension-generic), on `Ico a b`, with (i) chart-Gram jointly C∞ on the OPEN slab `Ioo a b ×ˢ chart-baseSet`, (ii) chart-Gram C⁰ on `Ico a b ×ˢ baseSet` (so only continuity at `t = a`), (iii) the pointwise Ricci-flow PDE `HasDerivWithinAt (fun s => (g s).inner x v w) (−2·Ric(g t)(v,w)) (Ici a) t` for every `t ∈ Ico a b` (one-sided at `a`), and (iv) `g₁ a = g₂ a`. Conclusion: `g₁ t = g₂ t` on `Ico a b`. Sole consumer: `MaximalTime.lean:264` (`extends_of_rmBounded`), instantiated with `g₁ := g_fam` (ambient flow, jointly smooth TWO-SIDEDLY near the restart time `t_star` provided `t_star` is chosen strictly interior) and `g₂ := rr(·−t_star)` where `rr` is output by the sibling black box (N) `ricci_flow_unif_existence` (uniform DeTurck short-time existence; currently promises only interior-C∞ + C⁰-up-to-0 + PDE).

Audited mathematical position (2026-07-24, distilled; sources on the pushed branch):
- The C⁰-at-the-edge class is the entire difficulty. GSM77's Ricci–DeTurck uniqueness runs in the class `A⁻¹g̃ ≤ g ≤ Ag̃`, `|∇̃g| + √t|∇̃²g| ≤ A` (maximum principle on `|g−ḡ|²`), and its RF corollary uses the harmonic-map heat flow gauge for flows smooth up to the initial time. From hypotheses (i)–(iv) no derivative rates at `a` follow: the pointwise edge PDE yields only per-direction improper-integral convergence of Ric. Prior in-repo audits (`ExtendViaUniqueness.md` §VERIFIED item 1, 07-18 execution record; `RicciEdgeBounds.md` routes 1–3) agree, and identify the only faithful route for the statement AS IS: Burkhardt-Guim arXiv:1907.13116 Thm 5.4 (uniqueness of regularizing flows from C⁰ data), which needs a rough-C⁰ Ricci–DeTurck solver with common horizon + C⁰ stability — a large new Koch–Lamm-type parabolic foundation (costed in `RicciEdgeBounds.md` §2026-07-19).
- The consumer does NOT need the C⁰-edge class: `rr` comes from a DeTurck construction whose honest textbook output for SMOOTH initial data is joint smoothness UP TO `t = 0` (`Ico`-slab), and `g_fam` is smooth two-sidedly at any strictly interior `t_star`. The C⁰-edge shape of (B) is an artifact of (N)'s conservatively weak output fields, not a consumer requirement.

Proposed statement surgery (needs your ruling #1):
- (N): strengthen the output regularity field to chart-Gram `ContMDiffOn … (Ico 0 τ₀ ×ˢ baseSet)` (from `Ioo` + separate C⁰). Faithfulness: standard quasilinear parabolic smoothing up to the initial corner for smooth data. COST: (N) is currently a `sorry` but its lane is actively discharging it via a maximal-regularity engine (interior joint smoothness is verified machinery there; see `MaxRegSolutionJointlySmooth.lean`); up-to-corner C∞ would add a compatibility/time-derivative bootstrap at `t = 0` to that lane. QUESTION: in the L²-maximal-regularity/Duhamel framework, is up-to-corner joint C∞ for smooth data genuinely routine (iterated time-derivative bootstrap, no corner compatibility obstruction for quasilinear RDT), or is there a known wall that would make the strengthened (N) materially harder than interior smoothness?
- (A) `ricci_flow_interior_restart` (proved, my file): conclusion `t_star ∈ Ioo α ω` (mechanical re-choice of `t_star`), thread the `Ico` field.
- (B): both flows' hypotheses become chart-Gram `ContMDiffOn … (Ico a b ×ˢ baseSet)`; PDE + initial equality unchanged. This is the textbook smooth class; consumer supply re-checked on both sides.
- Alternative if surgery is REJECTED: prove (B) as stated via the Burkhardt-Guim route (large).

Route options for smooth-class (B) (needs your ruling #2):
- (K) Kotschwar-style L² energy, ungauged, dimension-generic (Kotschwar, "An energy approach to the problem of uniqueness for the Ricci flow"; closed case, no weight): fix `b' < b`, background `ḡ := g₁(a)`; `h := g₁−g₂`, `A := Γ₁−Γ₂`, `S := Rm₁−Rm₂`; `E := ∫_M (|h|² + |A|² + |S|²)_ḡ dμ_ḡ`; `Ė ≤ CE + (2∫⟨S,Δ_{g₁}S⟩ + cross)`; IBP gives `−2∫|∇₁S|²` which absorbs the `∇A`/`∇S` cross terms by Cauchy–Schwarz; Gronwall from `E(a) = 0` (equality of metrics at `a` kills all three at `a` since they are smooth up to `a` after surgery). No parabolic solver, no HMF, no maximum principle. Needs: ∂ₜΓ and ∂ₜRm evolution identities (the Shi lane has the Rm heat equation — exact generality being inventoried), covariant difference calculus, the integration layer (0-sorry in-repo), differentiation under ∫ over closed M, divergence theorem, Gronwall (mathlib). QUESTION: with FIXED-ḡ norms and measure (not Kotschwar's moving g₁-norms), the IBP of `∫⟨S, Δ_{g₁}S⟩_ḡ dμ_ḡ` produces discrepancy terms from `∇₁ḡ ≠ 0` and the `g₁`-vs-ḡ measure mismatch — confirm these are slab-bounded and absorbable (i.e. the fixed-background formulation is sound), or should the energy use `g₁(t)`-norms with the ∂ₜ(norm,measure) terms instead?
- (G) The book route: HMF gauge per flow + RDT uniqueness + ODE gauge-back. In-repo assets (all proof-sorry-free, most source-only/unverified): the FULL tension algebra (`Pullback/HarmonicTension.lean`: `idTension g h = −deTurckVF g h`, `tension_eq_push`, `tension_eq_DT`; `Pullback/DeTurckNaturality.lean`: `connDiff_push`, `deTurckVF_push`), the RDT→RF pullback conditional on ODE-generation hypotheses (`HamiltonDeTurckPullback(.Flat).lean`), DeTurck-VF time regularity (`DeTurckVFTimeFamily.lean`), RDT-uniqueness local window + forward continuation (`Evolution/DeTurckUniqueWindow.lean`: `chartRD_local`, `chartRD_forward`, via the verified maximal-regularity strong-pair uniqueness lane), per-datum DeTurck existence PROVED (`ShortTime/DeTurckInitialDataExistence.lean:164`). The 0% frontier: short-window HMF existence w.r.t. a time-dependent domain metric + diffeomorphism persistence; a generic-(r,s) time-dependent tame fixed point (`TensorMaximalRegularity/TimeTameFixedPoint.lean`, built for the (r,s)=(1,0) HMF coordinate equation) exists source-only, but its geometric tension-field/Nemytskii producer is MISSING (per its `.md`), and `MovingMass.md` records that the C⁰-edge gauge carrier does not close — irrelevant after surgery, but the smooth-class HMF producer is still a genuine new quasilinear-system lane.
Energy-side asset inventory (Explore recon 2026-07-24, all in-repo):
- ∂ₜΓ identity EXISTS sorry-free, dimension-generic (`Evolution/Connection/Christoffel.lean`: `christoffelMetricVariation_hasDerivWithinAt` :119, RF form :397). Single-flow ∇ᵏRm heat tower EXISTS sorry-free (`Evolution/NablaRiemannHeatFull.lean` :391, `IteratedRmTowerHeatEq.lean`), but its downstream reaction/commutator bound files still carry sorries. **NO difference-tensor Rm evolution and NO fixed-background (g,Γ,Rm) triple energy exist** — that is Route (K)'s true gap: a new heavy tensor-calculus layer (∂ₜ(Rm₁−Rm₂) = Δ₁(Rm₁−Rm₂) + cross-metric commutator terms, plus the triple-energy assembly), no new PDE solving.
- CRITICAL FIND: a **DeTurck-gauge single-metric-difference L² energy skeleton EXISTS sorry-free** (`Analysis/Spectral/Intrinsic/DeTurck/MovingEdgeEnergy.lean`): `movingDiffEnergy` (:924) = ∫|g₁−g₀|²dμ_{g₀(t)}, `movingEnergy_rate` (:1378, exact HasDerivAt, differentiation under ∫ done), `movingEnergy_zero` (:1412) = the Gronwall closure taking as its ONE remaining hypothesis the coercivity bound `movingRate ≤ K·movingDiffEnergy`; a pairing toolkit toward that bound is built (`edgeArm_energy_le`, `edgePrincipal_half`, `edgeLower_pair_le`, `edgeCore_pair_le`, `edgePair_inner`). I.e. the repo's live design was: RDT uniqueness by L² energy (strict parabolicity ⟹ metric difference alone suffices; Kotschwar's triple exists precisely to compensate the ungauged degeneracy). So route (G) has TWO built RDT-uniqueness halves: the energy one (gap = the coercivity bound) and the max-reg window one (`DeTurckUniqueWindow`, source-only, gap = Lean verification).
- Infrastructure sorry-free: differentiation under ∫ over compact M (`Analysis/Integration/Measure/FiniteParametricIntegral.lean:24`), tensor IBP (`Analysis/Elliptic/ConnectionLaplacian/GreenIdentityAndIBP/TensorConnLapLoweredIBP.lean:252`), integral Gronwall (`Analysis/ODE/IntegralGronwall.lean:77`, scalar endpoint `edgeGronwall_zero`), fixed-background tensor norms + `metricCovDerivNorm`, sharp connection-difference Cauchy–Schwarz (`connDiffVec_norm_le`, `diffStep_norm_le`).

GitHub reference to inspect before answering:
- Branch: https://github.com/liao9yuan/differential-geometry/tree/codex/analytic-producers-e87b (tip = the latest `fwd-unique` commit; the remaining uncommitted local files all belong to the sibling (N)-session lane and are irrelevant to (B). NOTE: `RicciEdgeBounds.lean` is now VERIFIED green + axiom-clean; `DeTurckUniqueWindow.lean` and `MovingEdgeEnergy.lean` remain UNVERIFIED — blocked by three broken upstream modules (`SobolevScale/SmoothEmbedInj`, `DeTurck/SmoothPathHs`, `DeTurck/MetricDiffJoint`), under cross-lane coordination; and the `TimeTameFixedPoint` draft FAILED its first real build on (0,2)-hard-coded upstream Duhamel lemmas — its (r,s)-generic claim is currently false, which raises Route (G)'s cost estimate.)
- Relevant files:
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Geometry/Flow/RicciFlow/Evolution/ExtendViaUniqueness.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Geometry/Flow/RicciFlow/Evolution/ExtendViaUniqueness.md
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Geometry/Flow/RicciFlow/Evolution/RicciEdgeBounds.md
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Geometry/Flow/RicciFlow/Evolution/DeTurckUniqueWindow.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Geometry/Flow/RicciFlow/Pullback/HarmonicTension.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Geometry/Flow/RicciFlow/HamiltonDeTurckPullback.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Analysis/Parabolic/QuasiLinear/TensorMaximalRegularity/TimeTameFixedPoint.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Geometry/Flow/RicciFlow/MaximalTime.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Analysis/Spectral/Intrinsic/DeTurck/MovingEdgeEnergy.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Analysis/Spectral/Intrinsic/DeTurck/EdgeDifferenceEnergy.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Analysis/Spectral/Intrinsic/DeTurck/StrongSolutionUniqueness.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Geometry/Flow/RicciFlow/Evolution/Connection/Christoffel.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Geometry/Flow/RicciFlow/Evolution/NablaRiemannHeatFull.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Analysis/ODE/IntegralGronwall.lean
  - https://github.com/liao9yuan/differential-geometry/blob/codex/analytic-producers-e87b/DifferentialGeometry/Analysis/Integration/Measure/FiniteParametricIntegral.lean

Constraints:
- Preserve public APIs except the sanctioned surgery; the surgery must keep `MaximalTime.lean` assemblable (wiring re-checked on my side).
- Prefer small helper lemmas. Avoid broad refactors. Do not suggest blind automation as the main plan.
- The (B) proof must be dimension-generic (the consumer is dim-3 but the statement is generic and should stay so unless you rule otherwise).
- Give a prompt for only the next implementation step.

Tasks:
1. RULE on the statement surgery: smooth-class (B) + strengthened (N) vs keep-as-stated + Burkhardt-Guim. Include your assessment of the up-to-corner C∞ cost in the maximal-regularity framework (the sibling lane must co-sign; treat their cost honestly).
2. RULE between routes (K) and (G) for smooth-class (B), given the asset profiles above. If (K): confirm or repair the fixed-background formulation (the IBP discrepancy question), and cost the missing Rm-difference evolution honestly. If (G): pick the RDT-uniqueness half (MovingEdgeEnergy coercivity vs DeTurckUniqueWindow verification) and state the smallest honest decomposition of the HMF existence producer (tension-field Nemytskii for the (r,s)=(1,0) tame fixed point, diffeo persistence, gauge identity wiring — what order, what can be skipped).
3. State the smallest useful helper lemmas for the chosen route, in dependency order (the first 2–3 bricks precisely; e.g. for (K): the ∂ₜΓ-difference identity shape, the Rm-difference evolution inequality shape, the energy-differentiability lemma shape).
4. Give the proof strategy in Lean terms (which existing in-repo layers to consume; what NOT to rebuild).
5. Tell me what failure signal should make the agent stop and re-consult.
