# ArcLength

`velocity_mdiff_at` now supplies the pointwise tangent-total-space regularity
producer: an `(n + 1)`-times continuously manifold-differentiable uncurried
variation has an `n`-times continuously manifold-differentiable velocity field.
The local proof reuses the `mfderiv_apply` coordinate calculation; focused
verification passed.
