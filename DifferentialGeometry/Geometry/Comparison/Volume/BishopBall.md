# BishopBall

## `hypRadVol_zero`

At zero curvature, `hypSn 0 t = t`, so the radial density is `t ^ d`.
Integrating this density from `0` to `R` gives
`R ^ (d + 1) / (d + 1)` via the existing power-integral formula.

Focused verification and the named module refresh passed.
