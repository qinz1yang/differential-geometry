# BishopPolar notes

## 2026-07-18 local normal polar formula

- Added `normalImage_polar` for measurable subsets of the normal exponential
  source and `normalBall_polar` for a source-contained tangent ball. The latter
  writes Riemannian volume as sphere/radius integration of
  `normalChartDensity`.
- Focused verification and the exported module refresh passed without local
  warnings.
- Two distinct frontiers now remain. First, transverse `curveDensity` must be
  identified with normal-chart density up to a positive direction-dependent
  constant. Second, the repository has no cut-time definition, cut-locus
  measurability/nullity theorem, or almost-everywhere global normal
  parametrization from which the local formula can be globalized.
- Continuing without an architecture ruling would open both frontiers at once.
  Bishop--Gromov itself remains unstated and unproved (0%); its dedicated V2
  machinery is approximately 45% complete.
