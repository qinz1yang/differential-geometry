# MetricBallMonotone

## 2026-07-16

- Added `injOn_of_return`, the generic buffered-cover argument that upgrades
  local injectivity and a half-buffer approximate return map to injectivity on
  the covered set.
- The theorem needs only a pseudometric on the source; the cover index and the
  target type are unrestricted.
- Verification status: focused check passed.
