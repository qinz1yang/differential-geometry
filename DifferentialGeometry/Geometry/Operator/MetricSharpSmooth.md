# `MetricSharpSmooth.lean`

## Result

`trivToE_metricSharp` gives the fixed-chart inverse-Gram formula for the
metric sharp of a covector.  It keeps the moving-fiber object fully applied
and expresses the result as a finite scalar component sum.

## Verification and use

Focused verification and the targeted export refresh passed without warnings.
The regularized L-phase field uses this formula for both scalar-gradient and
Ricci-sharp coordinate terms.
