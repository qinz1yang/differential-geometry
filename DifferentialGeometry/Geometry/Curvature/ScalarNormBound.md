# ScalarNormBound

## Status

`scalar_abs_le_rm` is the invariant trace estimate from the canonical lowered
Riemann tensor norm to the absolute scalar curvature.  It is intended to turn
`FlowMetricBall.IsRmControlled` into the scalar upper bound consumed by the
Perelman cutoff W-form theorem.

Focused verification and the targeted module build passed without warnings in
the edited file.
